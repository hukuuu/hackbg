{
  "name": "3-A-Big-File",
  "version": "1.0.0",
  "description": "We want to use Node streams in order to read a big file, containing random numbers, separated by `,` and compute their sum. The file should be of size around `5GB`, so it wont be easy to load it in memory.",
  "main": "make-file.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nash": "^1.2.0",
    "optimist": "^0.6.1"
  }
}
